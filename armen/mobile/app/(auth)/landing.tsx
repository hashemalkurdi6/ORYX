// Landing route — first thing an unauthenticated user sees. Composes
// LandingScreen which owns the entry timeline + ambient halo.
import LandingScreen from '@/components/LandingScreen';

export default function Landing() {
  return <LandingScreen />;
}
